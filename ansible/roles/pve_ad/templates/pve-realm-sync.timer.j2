# Managed by ansible (pve_ad role). Periodic AD-realm sync.
[Unit]
Description=Periodic Proxmox {{ pve_ad_realm }} AD-realm sync

[Timer]
OnCalendar={{ pve_ad_sync_on_calendar }}
Persistent=true

[Install]
WantedBy=timers.target
