# Managed by ansible (pve_ldap role). Periodic LDAP-realm sync.
[Unit]
Description=Periodic Proxmox {{ pve_ldap_realm }} Authentik LDAP-realm sync

[Timer]
OnCalendar={{ pve_ldap_sync_on_calendar }}
Persistent=true

[Install]
WantedBy=timers.target
